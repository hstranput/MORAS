CHIP prviA {
    IN x, y;
    OUT out;

    PARTS:
    Not(in = x, out = nx);
    Not(in = y, out = ny);
    Or(a = nx, b = ny, out = out);
    
    
    
    
    
    
    
    
    

}